<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Application Theme -->
    <style name="Theme.Sonara" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <!-- Android 12+ splash: dark mode -->
    <style name="Theme.Sonara.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/sonara_petrol</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo_dark</item>
        <item name="postSplashScreenTheme">@style/Theme.Sonara</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>
</resources>
